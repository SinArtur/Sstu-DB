# 📚 Студенческая база знаний

Современная платформа для обмена учебными материалами с инвайт-системой регистрации и модерацией контента.

## ✨ Возможности

- 🔐 **Инвайт-система** — закрытая регистрация по приглашениям
- 👥 **Роли** — студент, модератор, администратор
- 🌳 **Древовидная структура** — Институт → Кафедра → Направление → Курс → Преподаватель
- 📤 **Загрузка файлов** — drag & drop, множественный выбор
- ⭐ **Рейтинг и комментарии** — оценка материалов и обсуждения
- ✅ **Модерация** — проверка контента перед публикацией
- 🔍 **Поиск** — по веткам, материалам и файлам
- 🔔 **Уведомления** — о модерации и новых материалах

## 🛠️ Технологии

**Backend:** Django 4.2.7 + DRF, PostgreSQL, JWT  
**Frontend:** React 18 + TypeScript, Tailwind CSS, Vite

## 🚀 Быстрый старт

```bash
# 1. Запуск контейнеров
docker-compose up -d

# 2. Создание тестовых данных
docker-compose exec backend python manage.py create_test_data

# 3. Открыть в браузере
# Frontend: http://localhost:3000
# Django Admin: http://localhost:8000/admin/
```

## 👤 Тестовые аккаунты

| Роль | Email | Пароль |
|------|-------|--------|
| Администратор | `admin@university.local` | `admin123` |
| Модератор | `moderator@university.local` | `mod123` |

> **Django Admin:** http://localhost:8000/admin/  
> Используйте учетные данные администратора для входа.

## 📖 Основные функции

### Регистрация
- Получите инвайт-токен от существующего пользователя
- Заполните форму с валидацией в реальном времени
- После регистрации студент получает 3 инвайт-токена

### Загрузка материалов
1. Выберите ветку в дереве
2. Нажмите "Загрузить материалы"
3. Перетащите файлы или выберите через диалог
4. Добавьте описание и теги
5. Материал отправится на модерацию

### Модерация
Модераторы и администраторы могут одобрять/отклонять материалы и запросы веток через раздел "Модерация".

## 🔧 Разработка

### Локальный запуск

**Backend:**
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

**Frontend:**
```bash
cd frontend
npm install
npm run dev
```

### Полезные команды

```bash
# Миграции
docker-compose exec backend python manage.py migrate
docker-compose exec backend python manage.py makemigrations

# Суперпользователь
docker-compose exec backend python manage.py createsuperuser

# Тесты
docker-compose exec backend python manage.py test
```

## 📁 Структура

```
SSTUDB/
├── backend/          # Django приложение
│   ├── accounts/     # Пользователи и инвайт-токены
│   ├── branches/     # Система веток
│   ├── materials/   # Материалы и файлы
│   ├── moderation/   # Модерация
│   └── notifications/# Уведомления
├── frontend/         # React приложение
└── docker-compose.yml
```

## 🔐 Безопасность

- JWT аутентификация
- Валидация паролей (минимум 8 символов, сложность)
- Проверка файлов перед загрузкой
- Модерация всего контента
- Инвайт-система для контроля регистрации

## 📚 API

Документация доступна через Swagger UI: http://localhost:8000/api/swagger/

**Основные endpoints:**
- `POST /api/auth/register/` - Регистрация
- `POST /api/auth/login/` - Вход
- `POST /api/auth/password-reset-request/` - Запрос восстановления пароля
- `POST /api/auth/password-reset-confirm/` - Подтверждение сброса пароля
- `POST /api/auth/verify-email/` - Подтверждение email
- `GET /api/branches/tree/` - Дерево веток
- `POST /api/branches/requests/` - Запрос ветки
- `POST /api/materials/` - Загрузка материалов
- `GET /api/materials/{id}/files/{file_id}/download/` - Скачивание файла
- `GET /api/search/?q=query` - Поиск

## 📧 Настройка отправки email

По умолчанию в режиме разработки письма выводятся в консоль. Для отправки реальных писем настройте SMTP:

1. Создайте файл `.env` в корне проекта (или настройте переменные окружения в Docker)
2. Добавьте следующие переменные:

```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@university.local
```

### Настройка Gmail:
1. Включите двухфакторную аутентификацию в аккаунте Google
2. Перейдите в [Настройки аккаунта → Безопасность](https://myaccount.google.com/security)
3. Создайте "Пароль приложения" и используйте его вместо обычного пароля

### Другие провайдеры:
- **Yandex**: `smtp.yandex.ru`, порт 465 (SSL) или 587 (TLS)
- **Mail.ru**: `smtp.mail.ru`, порт 465 (SSL) или 587 (TLS)
- **Outlook**: `smtp-mail.outlook.com`, порт 587 (TLS)

> **Важно:** Если SMTP настройки не указаны, система будет использовать консольный backend (письма выводятся в консоль).

## 🔑 Восстановление пароля

Пользователи могут восстановить пароль через специальную страницу:
1. На странице входа нажмите "Забыли пароль?"
2. Введите email (должен быть подтвержден)
3. Проверьте почту (включая папку "Спам")
4. Перейдите по ссылке из письма и установите новый пароль

> Ссылка для сброса пароля действительна в течение 24 часов.

## 🚢 Деплой

```bash
# Настройте .env файл с SMTP настройками
# Запустите production конфигурацию
docker-compose -f docker-compose.prod.yml up -d
```

> Файлы хранятся локально в директории `media/` на сервере.

---

**Разработано с ❤️ для студентов**
