# Инструкция по настройке модуля расписания СГТУ

Это руководство поможет вам настроить и запустить модуль расписания занятий.

## Что было добавлено

1. **Backend (Django):**
   - Новое приложение `schedule` с моделями для хранения данных о расписании
   - Парсер для автоматической загрузки расписания с сайта rasp.sstu.ru
   - API эндпоинты для работы с расписанием
   - Celery задачи для периодического обновления каждые 3 часа
   - Management команда для ручной синхронизации

2. **Frontend (React):**
   - Новая страница "Расписание" с красивым интерфейсом
   - Поиск и фильтрация по институтам, группам, дням недели
   - Выбор группы в профиле пользователя
   - Интеграция в навигацию приложения

3. **Модели данных:**
   - Institute (Институты СГТУ)
   - Group (Учебные группы)
   - Teacher (Преподаватели)
   - Subject (Предметы)
   - Lesson (Занятия)
   - ScheduleUpdate (История обновлений)

## Пошаговая настройка

### Шаг 1: Установка зависимостей

```bash
# Установите новые Python пакеты
pip install beautifulsoup4==4.12.2 requests==2.31.0
```

Или переустановите все зависимости:

```bash
cd backend
pip install -r requirements.txt
```

### Шаг 2: Применение миграций

```bash
cd backend
python manage.py migrate
```

Это создаст новые таблицы в базе данных:
- `schedule_institute`
- `schedule_group`
- `schedule_teacher`
- `schedule_subject`
- `schedule_lesson`
- `schedule_scheduleupdate`
- Добавит поле `group` в таблицу `accounts_user`

### Шаг 3: Первоначальная загрузка расписания

Загрузите расписание с сайта СГТУ:

```bash
python manage.py sync_schedule
```

**Внимание:** Эта команда может выполняться долго (несколько минут), так как парсит все институты и группы.

Если хотите загрузить расписание только для одной группы:

```bash
python manage.py sync_schedule --group 104
```

где `104` - это SSTU ID группы (можно узнать из URL на сайте rasp.sstu.ru)

### Шаг 4: Настройка Redis (для Celery)

Redis необходим для работы периодических задач.

**Windows:**
1. Скачайте Redis для Windows: https://github.com/microsoftarchive/redis/releases
2. Установите и запустите Redis
3. Или используйте Docker: `docker run -p 6379:6379 redis:alpine`

**Linux/macOS:**
```bash
# Ubuntu/Debian
sudo apt-get install redis-server
sudo service redis-server start

# macOS
brew install redis
brew services start redis
```

### Шаг 5: Запуск Celery

Celery нужен для автоматического обновления расписания каждые 3 часа.

Откройте **два отдельных терминала** в директории backend:

**Терминал 1 - Celery Worker:**
```bash
cd backend

# Windows
celery -A config worker -l info --pool=solo

# Linux/macOS
celery -A config worker -l info
```

**Терминал 2 - Celery Beat (планировщик):**
```bash
cd backend
celery -A config beat -l info
```

Вы должны увидеть сообщение о регистрации задачи `schedule.sync_all_schedules`.

### Шаг 6: Запуск приложения

**Backend:**
```bash
cd backend
python manage.py runserver
```

**Frontend:**
```bash
cd frontend
npm install  # если еще не установлено
npm run dev
```

### Шаг 7: Настройка пользователя

1. Откройте приложение: http://localhost:3000
2. Войдите в систему
3. Перейдите в "Профиль"
4. В разделе "Моя группа" выберите институт и группу
5. Нажмите "Сохранить группу"

Теперь в навигации появится ссылка "Расписание"!

## Использование

### Просмотр расписания

1. Перейдите в раздел "Расписание" через навигационное меню
2. Если вы указали группу в профиле, расписание загрузится автоматически
3. Если нет - выберите институт и группу в фильтрах

### Поиск и фильтрация

- **Поиск:** Введите название предмета, ФИО преподавателя или номер аудитории
- **День недели:** Выберите конкретный день для просмотра
- **Институт/Группа:** Переключайтесь между разными группами

### Ручное обновление расписания

Модераторы и администраторы могут запустить обновление вручную:

```bash
python manage.py sync_schedule
```

Или через API (требуется авторизация с ролью модератора/админа):
```bash
POST /api/schedule/updates/trigger_sync/
```

## Проверка работы Celery

Чтобы убедиться, что Celery работает правильно:

1. Проверьте логи Celery Worker - должно быть сообщение "ready"
2. Проверьте логи Celery Beat - должна быть зарегистрирована задача "sync-schedules-every-3-hours"
3. Посмотрите историю обновлений в админке Django: http://localhost:8000/admin/schedule/scheduleupdate/

## Docker (опционально)

Если вы используете Docker, добавьте сервисы в `docker-compose.yml`:

```yaml
services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  celery_worker:
    build: ./backend
    command: celery -A config worker -l info
    depends_on:
      - db
      - redis
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0

  celery_beat:
    build: ./backend
    command: celery -A config beat -l info
    depends_on:
      - db
      - redis
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0

volumes:
  redis_data:
```

Затем запустите:
```bash
docker-compose up -d
docker-compose exec backend python manage.py sync_schedule
```

## Troubleshooting

### Ошибка "connection to server at localhost failed"
- Проблема: PostgreSQL не запущен
- Решение: Запустите PostgreSQL или используйте SQLite для разработки

### Ошибка "Can't connect to Redis"
- Проблема: Redis не запущен
- Решение: Запустите Redis (`redis-server`) или установите его

### Celery не запускается на Windows
- Проблема: Celery 5.x имеет проблемы с Windows
- Решение: Используйте флаг `--pool=solo`: `celery -A config worker -l info --pool=solo`

### Расписание не загружается
- Проблема: Сайт СГТУ недоступен или изменилась структура HTML
- Решение: Проверьте доступность rasp.sstu.ru, посмотрите логи Django

### Миграции не применяются
- Проблема: Конфликт миграций
- Решение: 
  ```bash
  python manage.py migrate --fake schedule zero
  python manage.py migrate schedule
  ```

## API Documentation

### Получить список групп
```http
GET /api/schedule/groups/
```

### Получить расписание группы
```http
GET /api/schedule/lessons/?group=1
```

### Получить мое расписание
```http
GET /api/schedule/lessons/my_schedule/
```

### Недельное расписание
```http
GET /api/schedule/lessons/weekly/?group=1
```

Полная документация API доступна в Swagger: http://localhost:8000/api/swagger/

## Что дальше?

1. **Уведомления об изменениях:** Добавить уведомления при изменении расписания
2. **Экспорт:** Добавить экспорт в iCal/Google Calendar
3. **Напоминания:** Push-уведомления о предстоящих занятиях
4. **Статистика:** Аналитика посещаемости и нагрузки

## Поддержка

Если у вас возникли проблемы:
1. Проверьте логи Django и Celery
2. Убедитесь, что все сервисы (PostgreSQL, Redis) запущены
3. Проверьте, что миграции применены
4. Посмотрите документацию в `backend/schedule/README.md`

---

**Готово!** Теперь у вас работает полноценный модуль расписания с автоматическим обновлением! 🎉

