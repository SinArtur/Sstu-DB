# Гайд по настройке SSL сертификата для polikek.ru

## Шаг 1: Настройка DNS записей на reg.ru

1. Войдите в панель управления reg.ru
2. Перейдите в раздел **"Домены"**
3. Выберите домен **polikek.ru**
4. Перейдите в **"DNS-серверы и зона"**
5. Добавьте/измените следующие A-записи:

   **Запись 1:**
   - **Тип:** `A`
   - **Имя:** `@` (или оставьте пустым)
   - **Значение:** `IP_АДРЕС_ВАШЕГО_СЕРВЕРА` (узнайте командой `curl ifconfig.me` на сервере)
   - **TTL:** `3600`

   **Запись 2:**
   - **Тип:** `A`
   - **Имя:** `www`
   - **Значение:** `IP_АДРЕС_ВАШЕГО_СЕРВЕРА` (тот же IP)
   - **TTL:** `3600`

6. Сохраните изменения

**Проверка DNS:**
```bash
# На вашем локальном компьютере выполните:
nslookup polikek.ru
nslookup www.polikek.ru

# Оба должны возвращать IP адрес вашего сервера
```

⏰ **Важно:** Изменения DNS могут занять до 24 часов, но обычно применяются в течение 15-60 минут.

---

## Шаг 2: Установка Certbot (если еще не установлен)

```bash
# На сервере
apt update
apt install certbot -y
```

---

## Шаг 3: Получение SSL сертификата

### Вариант А: Использование автоматического скрипта

1. Перейдите в директорию проекта:
```bash
cd ~/opt/Sstu-DB
```

2. Отредактируйте скрипт и укажите ваш email:
```bash
nano scripts/setup-ssl.sh
# Измените строку: EMAIL="your-email@example.com"
```

3. Сделайте скрипт исполняемым и запустите:
```bash
chmod +x scripts/setup-ssl.sh
./scripts/setup-ssl.sh
```

### Вариант Б: Ручная настройка

1. **Временно остановите nginx:**
```bash
cd ~/opt/Sstu-DB
docker compose -f docker-compose.prod.yml stop nginx
```

2. **Создайте директорию для сертификатов:**
```bash
mkdir -p nginx/ssl
```

3. **Получите SSL сертификат:**
```bash
certbot certonly --standalone \
    -d polikek.ru \
    -d www.polikek.ru \
    --email ваш-email@example.com \
    --agree-tos \
    --non-interactive \
    --preferred-challenges http
```

4. **Скопируйте сертификаты в директорию проекта:**
```bash
cp /etc/letsencrypt/live/polikek.ru/fullchain.pem nginx/ssl/
cp /etc/letsencrypt/live/polikek.ru/privkey.pem nginx/ssl/

# Установите правильные права доступа
chmod 644 nginx/ssl/fullchain.pem
chmod 600 nginx/ssl/privkey.pem
```

5. **Запустите nginx обратно:**
```bash
docker compose -f docker-compose.prod.yml up -d nginx
```

---

## Шаг 4: Обновление конфигурации

1. **Обновите файл `.env`:**
```bash
nano .env
```

Найдите строку `ALLOWED_HOSTS` и измените на:
```env
ALLOWED_HOSTS=polikek.ru,www.polikek.ru,IP_АДРЕС_СЕРВЕРА
```

2. **Перезапустите backend:**
```bash
docker compose -f docker-compose.prod.yml restart backend
```

---

## Шаг 5: Проверка работы

1. **Проверьте статус контейнеров:**
```bash
docker compose -f docker-compose.prod.yml ps
```

Все контейнеры должны быть в статусе "Up" (не "Restarting").

2. **Проверьте логи nginx:**
```bash
docker compose -f docker-compose.prod.yml logs nginx
```

Не должно быть ошибок связанных с SSL сертификатами.

3. **Откройте сайт в браузере:**
   - `https://polikek.ru`
   - `https://www.polikek.ru`

Оба должны открываться с зеленым замочком (HTTPS).

---

## Шаг 6: Настройка автоматического обновления сертификатов

Сертификаты Let's Encrypt действительны 90 дней. Настройте автоматическое обновление:

1. **Создайте задачу в cron:**
```bash
crontab -e
```

2. **Добавьте строку (обновление каждый день в 3:00 утра):**
```
0 3 * * * certbot renew --quiet --deploy-hook "cd /opt/Sstu-DB && cp /etc/letsencrypt/live/polikek.ru/fullchain.pem nginx/ssl/ && cp /etc/letsencrypt/live/polikek.ru/privkey.pem nginx/ssl/ && docker compose -f docker-compose.prod.yml restart nginx"
```

---

## Решение проблем

### Ошибка: "Failed to obtain certificate"

**Причина:** DNS записи еще не применились или неправильно настроены.

**Решение:**
1. Проверьте DNS записи: `nslookup polikek.ru`
2. Убедитесь, что A-записи указывают на правильный IP
3. Подождите еще немного (до 1 часа)

### Ошибка: "Address already in use" при получении сертификата

**Причина:** Порт 80 уже занят.

**Решение:**
1. Убедитесь, что nginx остановлен: `docker compose -f docker-compose.prod.yml stop nginx`
2. Проверьте, что порт свободен: `netstat -tuln | grep :80`

### Nginx перезапускается (Restarting)

**Причина:** Не найдены SSL сертификаты или неправильные права доступа.

**Решение:**
1. Проверьте наличие файлов:
```bash
ls -la nginx/ssl/
```

2. Проверьте логи:
```bash
docker compose -f docker-compose.prod.yml logs nginx
```

3. Убедитесь, что права доступа правильные:
```bash
chmod 644 nginx/ssl/fullchain.pem
chmod 600 nginx/ssl/privkey.pem
```

### Сайт открывается, но показывает ошибку безопасности

**Причина:** Сертификат не соответствует домену или срок действия истек.

**Решение:**
1. Проверьте, что сертификат для правильного домена
2. Обновите сертификат: `certbot renew`

---

## Полезные команды

```bash
# Проверить срок действия сертификата
certbot certificates

# Обновить сертификат вручную
certbot renew

# Проверить конфигурацию nginx
docker compose -f docker-compose.prod.yml exec nginx nginx -t

# Перезагрузить nginx без остановки
docker compose -f docker-compose.prod.yml exec nginx nginx -s reload
```

---

## Готово! 🎉

После выполнения всех шагов ваш сайт будет доступен по адресу:
- **https://polikek.ru**
- **https://www.polikek.ru**

С SSL сертификатом от Let's Encrypt (бесплатный и автоматически обновляемый).

