# Пример файла .env для скрипта синхронизации расписания
# Скопируйте этот файл в .env и заполните своими данными

# URL вашего API (без слэша в конце)
API_BASE_URL=http://localhost:8000/api

# Токен доступа (Bearer token)
# Чтобы получить токен:
# 1. Войдите на сайт как администратор
# 2. Откройте консоль браузера (F12)
# 3. Перейдите на вкладку Application/Storage -> Local Storage
# 4. Найдите ключ 'auth-storage' и скопируйте значение accessToken
API_TOKEN=your_access_token_here

# Интервал синхронизации в часах (по умолчанию 3)
SYNC_INTERVAL_HOURS=3

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

